// code away!
// const server = require('./server.js')

// server.listen(4000, () => {
//   console.log('\n *** Server listening on localhost:4000 ***\n')
// })
const server = require('./server.js')

server.listen(4000, () => {
  console.log(' \n Server Running on localhost:4000 \n')
})